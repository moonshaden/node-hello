<%- include('partials/head', { description: page.summary }) %>

<div class="page-head">
  <div class="wrap narrow">
    <h1><%= page.title %></h1>
    <% if (page.summary) { %><p class="lede"><%= page.summary %></p><% } %>
  </div>
</div>

<section class="band">
  <div class="wrap narrow prose"><%- markdown.render(page.body) %></div>
</section>

<%- include('partials/foot') %>
